# Application settings live in this file. Real environment variables always override it.
# Do not commit .env (it is git-ignored); commit .env.example as the template.
#
# APP_ENV unset (or unknown) means "production", which requires `trunk build` first.
APP_NAME="%%title%%"
APP_ENV=local
APP_DEBUG=1
%%port_line%%
